version: 0.2

phases:
  install:
  
    runtime-versions:
      docker: 18
      
    commands:
      - npm install -g @angular/cli
      - npm install
      
  build:
    commands:
      - ng build
      - npm test
  post_build:
    commands:
      - echo Build completed
      
artifacts:
  files:
    - '**/*'
