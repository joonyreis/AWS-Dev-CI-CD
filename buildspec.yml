version: 0.2

phases:
  install:
    commands:
      - npm install -g @angular/cli
      - npm install
      - ng build
  build:
    commands:
      - ng build
      - npm test
artifacts:
  files:
    - '**/*'
